import { Homepage } from './components';
import { Route, Routes, HashRouter } from "react-router-dom";

function App() {
    return ( <
        div className = "App" >
        <
        Homepage / >
        <
        /div>
    );
}

export default App;